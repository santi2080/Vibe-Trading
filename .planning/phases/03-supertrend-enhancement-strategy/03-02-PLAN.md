---
phase: 03-supertrend-enhancement-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/analysis/supertrend_metrics.py
  - agent/tests/test_supertrend_enhancement_metrics.py
autonomous: true
requirements:
  - P03-R4
  - P03-R6
  - D-03-07
  - D-03-08
  - D-03-11
must_haves:
  truths:
    - "Every Phase 03 experiment can report trading-oriented metrics beyond direction accuracy."
    - "Metrics include win rate, profit factor, max drawdown, Sharpe/Sortino, CAGR/Calmar, trade count, holding bars/days, exposure, whipsaw count, and regime split."
    - "Transaction cost and slippage assumptions are configurable and visible in metrics/report rows."
  artifacts:
    - path: "agent/src/analysis/supertrend_metrics.py"
      provides: "Phase 03 diagnostics and report-row helpers layered on existing backtest metrics"
      exports: ["TradeDiagnosticsConfig", "calculate_phase03_trade_metrics", "calculate_regime_splits"]
    - path: "agent/tests/test_supertrend_enhancement_metrics.py"
      provides: "Focused tests for Phase 03 trading metrics, costs, exposure, whipsaw, and regime split"
  key_links:
    - from: "agent/src/analysis/supertrend_metrics.py"
      to: "backtest.metrics"
      via: "reuse existing win rate, profit factor, Sharpe, Sortino, Calmar, max drawdown helpers where available"
      pattern: "from backtest\.metrics import"
---

<objective>
Add the trading diagnostics foundation needed to judge SuperTrend enhancement strategies as strategies, not just indicators.

Purpose: Phase 02 direction accuracy and noise scores are insufficient for strategy quality. This plan creates reusable Phase 03 metric helpers that the strategy logic and experiment runner can share, per D-03-07 and D-03-08.
Output: `agent/src/analysis/supertrend_metrics.py` plus unit tests for cost/slippage-aware trade metrics, exposure, whipsaw count, and regime split diagnostics.
</objective>

<execution_context>
@/Users/iagent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/iagent/projects/vibe-trading/.planning/ROADMAP.md
@/Users/iagent/projects/vibe-trading/.planning/STATE.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-PATTERNS.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-01-SUMMARY.md
@/Users/iagent/projects/vibe-trading/agent/tests/test_metrics.py
@/Users/iagent/projects/vibe-trading/agent/backtest/metrics.py

<interfaces>
Existing metric foundation:
- `agent/tests/test_metrics.py` validates existing win rate, profit factor, Sharpe, Sortino, Calmar, max drawdown, trade count, average holding bars, symbol split, and exit split behavior.
- Phase 03 must not hand-roll duplicates of existing `backtest.metrics`; it should layer missing diagnostics on top.

New diagnostics contract:
- `TradeDiagnosticsConfig(initial_capital=100000.0, bars_per_year=252, transaction_cost_bps=5.0, slippage_bps=5.0, whipsaw_bars=5, whipsaw_loss_threshold=0.0)`.
- `calculate_phase03_trade_metrics(trades, equity_curve, positions=None, regime_by_bar=None, config=None) -> dict` returns required Phase 03 aggregate fields.
- `calculate_regime_splits(trades, equity_curve, regime_by_bar, config=None) -> dict[str, dict]` returns per-regime metric subsets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Phase 03 metrics contract with tests</name>
  <files>agent/tests/test_supertrend_enhancement_metrics.py</files>
  <behavior>
    - Aggregate metrics include `win_rate`, `profit_factor`, `max_drawdown`, `sharpe`, `sortino`, `cagr` or `annual_return`, `calmar`, `trade_count`, `avg_holding_bars`, `avg_holding_days`, `exposure`, `whipsaw_count`, `transaction_cost_bps`, and `slippage_bps`.
    - Conservative transaction cost/slippage defaults reduce net trade returns compared with zero-cost metrics.
    - Exposure is computed from the position series as invested bars divided by total bars.
    - Whipsaw count increases when trades reverse or exit within `whipsaw_bars` with non-positive net result.
    - Regime split returns independent metric dictionaries for ADX/Chop/ATR or named regime buckets.
  </behavior>
  <action>Create `agent/tests/test_supertrend_enhancement_metrics.py` using deterministic synthetic trades/equity fixtures modeled after `agent/tests/test_metrics.py`. Tests must require existing metric reuse where available, cost/slippage visibility per default assumption D-03-11, and regime split/whipsaw diagnostics per D-03-08. Do not depend on real market data or generated reports.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail until a Phase 03 diagnostics helper emits all required metric names and visibly applies configurable cost/slippage assumptions.</acceptance_criteria>
  <done>The Phase 03 metric contract is executable under `agent/tests/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reusable trade diagnostics and regime split helpers</name>
  <files>agent/src/analysis/supertrend_metrics.py, agent/tests/test_supertrend_enhancement_metrics.py</files>
  <action>Implement `agent/src/analysis/supertrend_metrics.py` with typed dataclasses and pure functions. Reuse `backtest.metrics` for existing metrics instead of duplicating formulas per research `Don't Hand-Roll`; add only missing Phase 03 diagnostics: configurable transaction cost/slippage adjustment, exposure, whipsaw count, average holding days alias, and regime split summaries. Accept trades as existing `TradeRecord` objects or dict-like records if tests establish both shapes; keep output plain dictionaries suitable for CSV/Markdown rows. Use conservative defaults of 5 bps transaction cost and 5 bps slippage unless the caller passes overrides per D-03-11.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_metrics.py agent/tests/test_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>Helper output contains all required Phase 03 metrics, cost/slippage settings are represented in output, zero-trade cases return safe zeros/NaNs without crashing, and existing `agent/tests/test_metrics.py` still passes.</acceptance_criteria>
  <done>Phase 03 trading diagnostics are reusable by strategy tests and the experiment runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add report-row serialization helpers for runner integration</name>
  <files>agent/src/analysis/supertrend_metrics.py, agent/tests/test_supertrend_enhancement_metrics.py</files>
  <action>Add a small serialization helper, such as `flatten_phase03_metrics(metrics: dict, prefix: str = "") -> dict`, only if needed by tests to produce stable CSV-compatible scalar fields. Ensure regime split values are flattened with predictable keys like `regime_trending_profit_factor` and `regime_choppy_whipsaw_count`. Keep this module independent of file I/O; report writing belongs to the runner plan.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>Runner-facing metric dictionaries contain stable scalar keys suitable for `pandas.DataFrame(rows).to_csv(...)` without embedding non-serializable objects.</acceptance_criteria>
  <done>Metrics can be consumed directly by the Phase 03 experiment runner without adding metric-specific serialization logic there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trade/equity records to diagnostics | Strategy outputs enter metric calculations and may be empty, malformed, or inconsistent. |
| Diagnostics to reports | Metric dictionaries become CSV/Markdown content and must not include secrets or non-serializable objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | metric inputs | mitigate | Validate expected trade fields, handle empty inputs deterministically, and expose cost/slippage parameters in output. |
| T-03-02-02 | Information Disclosure | report-row helpers | accept | Metrics contain strategy diagnostics only; no environment variables or secrets are read. |
| T-03-02-03 | Denial of Service | regime split grouping | mitigate | Use simple bounded groupby over provided trades/bars; no recursive or external calls. |
| T-03-02-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_metrics.py agent/tests/test_metrics.py -q</automated>
</verification>

<success_criteria>
- Trading evaluation metrics required by Phase 03 are available as reusable helpers.
- Cost/slippage assumptions are configurable with conservative defaults and visible in outputs.
- Regime split and whipsaw diagnostics are tested.
- The module performs no file I/O and adds no external dependencies.
</success_criteria>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-02-SUMMARY.md` when done.
</output>
