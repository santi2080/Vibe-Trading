---
phase: 03-supertrend-enhancement-strategy
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - agent/src/analysis/supertrend_enhancement.py
  - agent/tests/test_supertrend_enhancement_strategy.py
autonomous: true
requirements:
  - P03-R2
  - P03-R3
  - D-03-01
  - D-03-02
  - D-03-03
  - D-03-04
  - D-03-06
  - D-03-09
  - D-03-10
  - D-03-11
must_haves:
  truths:
    - "Enhanced strategy feature logic combines completed weekly SuperTrend anchor with daily confirmation, regime filters, entry trigger families, and MTES conflict handling."
    - "Daily RangeFilter is the first daily confirmation candidate, and MTES is a conflict/interpretability filter rather than the primary trend indicator."
    - "Trading mode and cost/slippage assumptions are configurable with ETF/stocks long-only and futures long-short defaults."
  artifacts:
    - path: "agent/src/analysis/supertrend_enhancement.py"
      provides: "Reusable Phase 03 combination feature and signal generation logic"
      exports: ["EnhancementConfig", "resolve_trading_mode", "build_confirmation_features", "build_regime_features", "build_entry_trigger_features", "build_enhancement_features", "generate_enhancement_signals", "build_experiment_matrix"]
    - path: "agent/tests/test_supertrend_enhancement_strategy.py"
      provides: "Unit tests for weekly anchor, daily RangeFilter confirmation, regime filters, entry families, MTES conflict filter, and trading mode defaults"
  key_links:
    - from: "agent/src/analysis/supertrend_enhancement.py"
      to: "agent/src/analysis/supertrend.py"
      via: "calculate_supertrend and align_completed_weekly_supertrend"
      pattern: "align_completed_weekly_supertrend|calculate_supertrend"
    - from: "agent/src/analysis/supertrend_enhancement.py"
      to: "src.analysis.major_trend_evaluator"
      via: "mtes_frame input with conflict/direction/regime columns produced by runner from MajorTrendEvaluator or existing MTES patterns"
      pattern: "mtes_conflict|mtes_direction|mtes_regime|timeframe_conflict"
---

<objective>
Implement the reusable SuperTrend enhancement feature and signal layer for Phase 03.

Purpose: Move from standalone SuperTrend direction labels to strategy-family feature logic: completed weekly SuperTrend anchor, daily RangeFilter confirmation, ADX/Chop/ATR/regime gates, EMA pullback, breakout, RSI/MACD recovery triggers, and MTES conflict filtering per locked decisions.
Output: A reusable analysis module with deterministic unit tests. Validation documentation is handled separately by `03-04-PLAN.md` so this plan remains small and executable.
</objective>

<execution_context>
@/Users/iagent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/iagent/projects/vibe-trading/.planning/ROADMAP.md
@/Users/iagent/projects/vibe-trading/.planning/STATE.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-PATTERNS.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-VALIDATION.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-01-SUMMARY.md
@/Users/iagent/projects/vibe-trading/agent/src/analysis/supertrend.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/trend.py
@/Users/iagent/projects/vibe-trading/agent/tests/test_mtes_strategy.py

<interfaces>
Inputs from 03-01:
- `calculate_supertrend(df, config)` and `align_completed_weekly_supertrend(daily_df, weekly_df, config)` exist in `agent/src/analysis/supertrend.py`.

New combination contract:
- `EnhancementConfig` includes `trading_mode: str = "auto"`, `transaction_cost_bps: float = 5.0`, `slippage_bps: float = 5.0`, ST/RF/ADX/Chop/ATR/EMA/MACD/RSI/breakout parameter fields, and booleans for `use_range_filter`, `use_regime_filter`, `use_mtes_conflict_filter`.
- `resolve_trading_mode(market: str, config: EnhancementConfig) -> str` returns `long_only` for stock/ETF/A-share/US-stock markets and `long_short` for futures by default.
- `build_confirmation_features(daily_df, config=None) -> pd.DataFrame` adds daily RangeFilter and EMA context columns.
- `build_regime_features(daily_df, config=None) -> pd.DataFrame` adds ADX, Choppiness, ATR percentile, and trend-efficiency flags/scores.
- `build_entry_trigger_features(daily_df, config=None) -> pd.DataFrame` adds pullback, breakout, RSI recovery, and MACD recovery trigger columns.
- `build_enhancement_features(daily_df, weekly_df=None, market="stock", mtes_frame=None, config=None) -> pd.DataFrame` combines anchor, confirmation, regime, trigger, conflict, and parameter columns.
- `generate_enhancement_signals(features, entry_family: str, config=None) -> pd.Series` emits `-1/0/1` signals without execution sizing.
- `build_experiment_matrix() -> list[dict]` names baselines and combinations for E1-E8 at minimum.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin enhancement signal behavior with deterministic tests</name>
  <files>agent/tests/test_supertrend_enhancement_strategy.py</files>
  <behavior>
    - Completed weekly ST bullish anchor plus daily RangeFilter bullish confirmation permits long entries; disagreement blocks entries.
    - In long-only mode, bearish conditions produce neutral signals rather than shorts; in long-short mode, bearish anchor/confirmation can produce short signals.
    - ADX/Chop/ATR/trend-efficiency regime filters block entries in choppy or extreme-volatility fixtures.
    - EMA pullback, breakout, RSI recovery, and MACD recovery are separate entry families with independently testable fixture triggers.
    - MTES conflict frame rejects or down-ranks trades when MTES conflicts with weekly ST or daily trigger; MTES is not the primary trend source.
  </behavior>
  <action>Create `agent/tests/test_supertrend_enhancement_strategy.py` with deterministic daily/weekly OHLCV fixtures and minimal synthetic MTES conflict columns. Write tests for weekly ST anchor consumption, daily RangeFilter confirmation, ADX/Chop/ATR/regime gating, EMA pullback, Donchian breakout, RSI recovery, MACD recovery, MTES conflict frame behavior, and market-specific trading-mode defaults per D-03-02, D-03-03, D-03-04, D-03-09, D-03-10, and D-03-11. Unit tests must not read real data or generated reports.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_strategy.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail until strategy features expose anchor, RangeFilter, regime, entry-family, MTES conflict, trading mode, and cost/slippage fields with the locked Phase 03 semantics.</acceptance_criteria>
  <done>Strategy combination behavior is executable as deterministic tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement confirmation, regime, and entry trigger feature builders</name>
  <files>agent/src/analysis/supertrend_enhancement.py, agent/tests/test_supertrend_enhancement_strategy.py</files>
  <action>Implement `EnhancementConfig`, `resolve_trading_mode`, `build_confirmation_features`, `build_regime_features`, and `build_entry_trigger_features`. Add daily RangeFilter direction calculation by reusing Phase 02 semantics without importing untracked script-only classes. Add EMA context, ADX, Choppiness Index, ATR percentile, trend-efficiency regime flags/scores, and entry trigger columns for EMA pullback, Donchian breakout, RSI recovery, and MACD recovery. Keep every trigger as a separate boolean column and do not generate final signals in this task.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_strategy.py agent/tests/test_supertrend_calculation.py -q</automated>
  </verify>
  <acceptance_criteria>Feature output includes `rf_direction`, EMA context, ADX/Chop/ATR/trend-efficiency regime fields, `entry_pullback`, `entry_breakout`, `entry_rsi_recovery`, `entry_macd_recovery`, `trading_mode`, `transaction_cost_bps`, and `slippage_bps`; tests pass without new dependencies.</acceptance_criteria>
  <done>Confirmation, regime, and entry trigger features are reusable and tested independently of final signal generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement anchor/conflict-aware feature merge and signal generation</name>
  <files>agent/src/analysis/supertrend_enhancement.py, agent/tests/test_supertrend_enhancement_strategy.py</files>
  <action>Implement `build_enhancement_features`, `generate_enhancement_signals`, and `build_experiment_matrix`. Use canonical helpers from `agent/src/analysis/supertrend.py` for completed weekly ST anchor. Merge confirmation, regime, entry trigger, and optional `mtes_frame` columns (`mtes_direction`, `mtes_regime`, `mtes_conflict`, `timeframe_conflict` when present). Generate signals only from completed weekly ST anchor plus daily confirmation plus selected entry family plus allowed regime plus no MTES conflict. Enforce `long_only` vs `long_short` behavior from `resolve_trading_mode`. Include matrix rows for baselines and E3-E8 combinations but leave real MTES metadata derivation and trade simulation to `03-05` runner. Keep position sizing, live trading, and broker integration out of scope.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_strategy.py agent/tests/test_supertrend_calculation.py -q</automated>
  </verify>
  <acceptance_criteria>Feature output includes `weekly_st_trend_completed`, `rf_direction`, regime flags/scores, `entry_pullback`, `entry_breakout`, `entry_rsi_recovery`, `entry_macd_recovery`, `mtes_conflict`, `trading_mode`, `transaction_cost_bps`, `slippage_bps`, and selected `signal`; all tests pass without new dependencies.</acceptance_criteria>
  <done>Reusable combination logic exists for the experiment runner and implements the Phase 03 strategy stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature inputs to signal layer | Daily/weekly OHLCV and optional MTES metadata enter strategy feature logic. |
| Signal layer to downstream runner | Generated signals and feature columns will drive backtest trades and reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Integrity | weekly ST anchor feature | mitigate | Use 03-01 completed-week helper and run no-lookahead tests with strategy tests. |
| T-03-03-02 | Tampering | strategy config | mitigate | Validate `entry_family`, `trading_mode`, threshold values, and cost/slippage defaults. |
| T-03-03-03 | Integrity | MTES conflict frame | mitigate | Accept explicit `mtes_frame` columns and test conflict-veto behavior; runner derives real metadata in 03-05. |
| T-03-03-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_strategy.py agent/tests/test_supertrend_calculation.py -q</automated>
</verification>

<success_criteria>
- Strategy feature logic implements weekly ST anchor, daily RF confirmation, ADX/Chop/ATR/regime filters, EMA pullback, breakout, RSI/MACD recovery, and MTES conflict-frame filtering.
- Trading mode is configurable with long-only defaults for ETF/stocks and long-short defaults for futures.
- Cost/slippage assumptions are configurable and exposed in feature output.
- `03-03` remains focused on feature/signal logic; validation documentation and runner integration are handled by dependent plans.
</success_criteria>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-03-SUMMARY.md` when done.
</output>
