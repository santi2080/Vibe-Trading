---
phase: 03-supertrend-enhancement-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/analysis/supertrend.py
  - agent/tests/test_supertrend_calculation.py
autonomous: true
requirements:
  - P03-R1
  - P03-R2
  - D-03-01
  - D-03-05
  - D-03-06
must_haves:
  truths:
    - "SuperTrend calculation exposes stateful final upper/lower bands, trend state, and contiguous supertrend line."
    - "SuperTrend warmup/convergence bars are explicit and removable before metrics."
    - "Weekly SuperTrend anchor aligned to daily bars uses only completed weekly bars; same-week unfinished weekly state never appears in daily decisions."
  artifacts:
    - path: "agent/src/analysis/supertrend.py"
      provides: "Canonical SuperTrend calculation and completed-week alignment helpers"
      exports: ["SuperTrendConfig", "calculate_supertrend", "remove_supertrend_warmup", "align_completed_weekly_supertrend"]
    - path: "agent/tests/test_supertrend_calculation.py"
      provides: "Deterministic tests for final bands, warmup, no future data use, and weekly-to-daily no-lookahead alignment"
  key_links:
    - from: "agent/src/analysis/supertrend.py"
      to: "agent/backtest/strategies/mtf.py"
      via: "MTFAligner(MTFConfig(lag_bars=1)).align_htf_to_ltf or equivalent completed-week shift"
      pattern: "lag_bars=1|shift\(1\)"
---

<objective>
Standardize SuperTrend into one reusable, tested implementation and make completed-week weekly-to-daily alignment executable.

Purpose: Phase 03 depends on SuperTrend as a higher-timeframe anchor, but Phase 02 used simplified SuperTrend semantics. This plan locks the corrected algorithm before any strategy combinations use it, per D-03-01 and D-03-05.
Output: `agent/src/analysis/supertrend.py` plus focused pytest coverage for final upper/lower bands, trend state transitions, warmup, no future-bar use, and weekly anchor alignment.
</objective>

<execution_context>
@/Users/iagent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/iagent/projects/vibe-trading/.planning/ROADMAP.md
@/Users/iagent/projects/vibe-trading/.planning/STATE.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-PATTERNS.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-01-SUMMARY.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-VERIFICATION.md
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/mtf.py
@/Users/iagent/projects/vibe-trading/agent/tests/test_major_trend_evaluator.py

<interfaces>
Existing project contracts to use directly:
- Tests live under `agent/tests/`; use `.venv/bin/python3 -m pytest agent/tests/...`.
- `MTFAligner(MTFConfig(lag_bars=1)).align_htf_to_ltf(...)` is the project pattern for completed higher-timeframe alignment.
- `BaseStrategy.generate(df)` expects indicator `pd.Series` aligned to the input OHLCV index and strategy signals in `-1, 0, 1`.

New canonical SuperTrend contract created by this plan:
- `SuperTrendConfig(period: int = 10, multiplier: float = 3.0, warmup_extra: int = 100, atr_method: str = "wilder")`.
- `calculate_supertrend(df: pd.DataFrame, config: SuperTrendConfig | None = None) -> pd.DataFrame` returns input-aligned columns `st_atr`, `st_basic_upper`, `st_basic_lower`, `st_final_upper`, `st_final_lower`, `st_trend`, and `supertrend`.
- `remove_supertrend_warmup(result: pd.DataFrame, config: SuperTrendConfig | None = None) -> pd.DataFrame` removes at least `period + warmup_extra` rows from SuperTrend metrics per research warmup requirement.
- `align_completed_weekly_supertrend(daily_df: pd.DataFrame, weekly_df: pd.DataFrame, config: SuperTrendConfig | None = None) -> pd.DataFrame` adds completed-week anchor columns to daily rows without same-week leakage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin canonical SuperTrend behavior with deterministic tests</name>
  <files>agent/tests/test_supertrend_calculation.py</files>
  <behavior>
    - Final upper/lower bands carry forward statefully instead of using only basic bands.
    - Trend flips only when close crosses the relevant final band; `st_trend` remains in `{-1, 1}` after warmup.
    - `supertrend` equals final lower band in bullish state and final upper band in bearish state.
    - Warmup removal drops at least `period + 100` rows by default.
    - Mutating or appending future bars does not change already-computed past SuperTrend values.
    - Missing `high`, `low`, or `close` raises a clear `ValueError`.
  </behavior>
  <action>Create `agent/tests/test_supertrend_calculation.py` with deterministic OHLCV fixtures for rising, falling, choppy, and short-data cases. Write tests before implementation per D-03-05. Assert the public contract for `SuperTrendConfig`, `calculate_supertrend`, and `remove_supertrend_warmup`; assert no future-function behavior by calculating a fixture, changing only rows after a cutoff, recalculating, and comparing all SuperTrend columns up to the cutoff. Do not use root `tests/`, and do not add external packages.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_calculation.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail until a canonical implementation emits `st_final_upper`, `st_final_lower`, `st_trend`, `supertrend`, enforces warmup handling, and proves future-row edits cannot alter past indicator values.</acceptance_criteria>
  <done>The calculation tests exist under `agent/tests/` and encode the corrected SuperTrend contract required by Phase 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical SuperTrend calculation module</name>
  <files>agent/src/analysis/supertrend.py, agent/tests/test_supertrend_calculation.py</files>
  <action>Implement `agent/src/analysis/supertrend.py` using pandas/numpy only. Normalize OHLC column names consistently with existing evaluator patterns, validate required `high`, `low`, and `close`, sort the index, and preserve index alignment. Use Wilder-style ATR smoothing via `ewm(alpha=1 / period, adjust=False, min_periods=period)` unless tests explicitly document a project alternative. Implement stateful final band carry-forward, `st_trend`, and `supertrend` columns per P03-R1. Keep the old Phase 02 simplified SuperTrend semantics out of this module; this is the canonical corrected implementation per D-03-01.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_calculation.py -q</automated>
  </verify>
  <acceptance_criteria>`calculate_supertrend` passes all deterministic tests, returns no execution or sizing columns, uses no external dependencies, and provides explicit warmup trimming through `remove_supertrend_warmup`.</acceptance_criteria>
  <done>`agent/src/analysis/supertrend.py` is the single reusable SuperTrend implementation for Phase 03 calculation logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add completed-week weekly SuperTrend to daily alignment tests and helper</name>
  <files>agent/src/analysis/supertrend.py, agent/tests/test_supertrend_calculation.py</files>
  <behavior>
    - A daily row inside week N sees the completed weekly SuperTrend state from week N-1, not the final state of week N.
    - Weekly state changes become visible to daily rows only after the weekly bar is complete.
    - Alignment preserves daily index length and adds clearly named completed anchor columns.
  </behavior>
  <action>Extend the test file with a weekly/daily fixture where the current incomplete week would flip SuperTrend if leaked. Implement `align_completed_weekly_supertrend` using the project completed-bar alignment pattern per D-03-06: prefer `MTFAligner(MTFConfig(lag_bars=1)).align_htf_to_ltf` when compatible, otherwise use an explicit completed-week shift that tests prove equivalent. The returned daily DataFrame must include `weekly_st_trend_completed`, `weekly_supertrend_completed`, `weekly_st_final_upper_completed`, and `weekly_st_final_lower_completed`. Do not use raw same-week `ffill` into daily decisions.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_calculation.py -q</automated>
  </verify>
  <acceptance_criteria>Tests demonstrate no same-week leakage and the helper provides daily-aligned completed weekly anchor columns for downstream strategy logic.</acceptance_criteria>
  <done>Weekly SuperTrend anchor alignment is reusable and no-lookahead tested before any enhanced strategy consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OHLCV DataFrame to indicator module | Untrusted local data columns and indexes enter calculation logic. |
| Weekly bars to daily decisions | Higher-timeframe information can leak into lower-timeframe strategy rows if alignment is wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `calculate_supertrend` input columns | mitigate | Validate required columns, normalize names, sort index, and raise clear `ValueError` on missing inputs. |
| T-03-01-02 | Integrity | weekly-to-daily alignment | mitigate | Add deterministic no-lookahead tests where same-week leakage would change daily signals. |
| T-03-01-03 | Denial of Service | SuperTrend loops | accept | Vectorization is partial; fixtures and intended research universes are bounded by Phase 02 25-symbol scope. |
| T-03-01-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_calculation.py -q</automated>
</verification>

<success_criteria>
- Corrected SuperTrend final upper/lower band, trend state, warmup, and no-future-function behavior are automated-test verified.
- Completed weekly SuperTrend to daily alignment is automated-test verified and cannot use unfinished weekly bars.
- No new external dependencies are introduced.
- Stable SuperTrend calculation lives in `agent/src/analysis/supertrend.py` for reuse by strategy and runner plans.
</success_criteria>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-01-SUMMARY.md` when done.
</output>
