---
phase: 03-supertrend-enhancement-strategy
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - scripts/backtest_supertrend_enhancement.py
  - agent/tests/test_supertrend_enhancement_runner.py
autonomous: true
requirements:
  - P03-R1
  - P03-R2
  - P03-R3
  - P03-R4
  - P03-R5
  - P03-R6
  - D-03-02
  - D-03-03
  - D-03-04
  - D-03-06
  - D-03-07
  - D-03-08
  - D-03-09
  - D-03-10
  - D-03-11
must_haves:
  truths:
    - "Experiment runner compares baselines buy-and-hold, legacy Phase 02 SuperTrend, corrected daily SuperTrend, weekly ST anchor, daily RF, and enhanced combinations on the same samples."
    - "Runner writes timestamped CSV and Markdown reports under `reports/` without asking users to commit all generated artifacts."
    - "Runner smoke tests prove CLI argument validation, no-lookahead integration, configurable trading mode/costs, and required report columns."
    - "Runner derives or loads real MTES conflict metadata per symbol/bar and reports conflict/veto counts for MTES-filtered experiments."
  artifacts:
    - path: "scripts/backtest_supertrend_enhancement.py"
      provides: "Phase 03 research experiment runner CLI and report generator"
      exports: ["main", "run_symbol_experiments", "generate_reports"]
    - path: "agent/tests/test_supertrend_enhancement_runner.py"
      provides: "Runner smoke and report-contract tests"
    - path: "reports/supertrend_enhancement_comparison_*.csv"
      provides: "Generated comparison report from runner execution"
    - path: "reports/supertrend_enhancement_report_*.md"
      provides: "Generated Markdown report from runner execution"
  key_links:
    - from: "scripts/backtest_supertrend_enhancement.py"
      to: "agent/src/analysis/supertrend_enhancement.py"
      via: "build_enhancement_features/generate_enhancement_signals/build_experiment_matrix"
      pattern: "build_enhancement_features|generate_enhancement_signals|build_experiment_matrix"
    - from: "scripts/backtest_supertrend_enhancement.py"
      to: "agent/src/analysis/supertrend_metrics.py"
      via: "calculate_phase03_trade_metrics"
      pattern: "calculate_phase03_trade_metrics"
    - from: "scripts/backtest_supertrend_enhancement.py"
      to: "agent/src/analysis/major_trend_evaluator.py"
      via: "derive_mtes_conflict_frame per symbol/bar and pass as mtes_frame to build_enhancement_features"
      pattern: "MajorTrendEvaluator|mtes_frame|mtes_conflict|vetoed_entry_count"
---

<objective>
Build the Phase 03 experiment runner that executes the SuperTrend enhancement matrix and generates comparison reports.

Purpose: Turn the tested calculation, combination, and metric foundations into reproducible research outputs comparing baselines and enhanced strategy families without declaring superiority before evidence.
Output: `scripts/backtest_supertrend_enhancement.py`, runner tests, and reproducible commands that generate `reports/supertrend_enhancement_comparison_*.csv` and `reports/supertrend_enhancement_report_*.md`.
</objective>

<execution_context>
@/Users/iagent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/iagent/projects/vibe-trading/.planning/ROADMAP.md
@/Users/iagent/projects/vibe-trading/.planning/STATE.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-PATTERNS.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-01-SUMMARY.md
@/Users/iagent/projects/vibe-trading/.planning/phases/02-trend-indicator-backtest/02-VERIFICATION.md
@/Users/iagent/projects/vibe-trading/scripts/backtest_trend_indicators.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/supertrend.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/supertrend_enhancement.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/supertrend_metrics.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py

<interfaces>
Inputs from prior plans:
- 03-01 provides canonical corrected SuperTrend and completed weekly alignment.
- 03-02 provides Phase 03 trading metrics and serialization helpers.
- 03-03 provides enhancement feature/signal generation and experiment matrix definitions.

Runner CLI contract:
- `.venv/bin/python3 scripts/backtest_supertrend_enhancement.py --symbol GC=F --matrix smoke --output reports`
- `.venv/bin/python3 scripts/backtest_supertrend_enhancement.py --all --matrix core --output reports`
- Optional flags: `--market-filter`, `--start`, `--end`, `--mode long_only|long_short|auto`, `--transaction-cost-bps`, `--slippage-bps`, `--entry-family`, `--walk-forward`, `--max-grid-size`.

Report-row contract:
- Required identity columns: `experiment_id`, `strategy_name`, `baseline_family`, `symbol`, `market`, `timeframe`, `entry_family`, `trading_mode`, `parameters`, `sample_start`, `sample_end`.
- Required baseline rows: `buy_and_hold`, `legacy_phase02_supertrend`, `corrected_daily_supertrend`, `completed_weekly_supertrend_anchor`, `daily_rangefilter`, and enhanced combinations.
- Required legacy-vs-corrected comparison fields: `st_algorithm_family`, `legacy_vs_corrected_direction_delta`, `legacy_vs_corrected_trade_delta`, `warmup_bars_removed`, and `algorithm_delta_notes`.
- Required feature columns: `uses_weekly_st_anchor`, `uses_daily_rf_confirmation`, `uses_regime_filter`, `uses_mtes_conflict_filter`, `mtes_conflict_count`, `mtes_vetoed_entry_count`.
- Required metrics: `win_rate`, `profit_factor`, `max_drawdown`, `sharpe`, `sortino`, `cagr` or `annual_return`, `calmar`, `trade_count`, `avg_holding_bars`, `avg_holding_days`, `exposure`, `whipsaw_count`, `transaction_cost_bps`, `slippage_bps`, and regime split columns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin runner CLI and report contract tests</name>
  <files>agent/tests/test_supertrend_enhancement_runner.py</files>
  <behavior>
    - Runner parser accepts symbol/all, market filter, matrix mode, output path, trading mode, transaction cost, slippage, entry family, walk-forward, and max grid size flags.
    - Smoke execution can run on a tiny synthetic in-test fixture without real market data.
    - Report rows include all required baseline/combo identity, feature, and metric columns.
    - Output path validation defaults to project `reports/` and rejects traversal outside the project unless explicitly safe-resolved.
  </behavior>
  <action>Create `agent/tests/test_supertrend_enhancement_runner.py` with tests that import runner functions directly rather than shelling out for every assertion. Use monkeypatch or temporary fixture data to avoid relying on real local Parquet in unit tests. Require baselines for buy-and-hold, legacy Phase 02 simplified SuperTrend, corrected daily ST, completed weekly ST anchor, daily RangeFilter, and enhanced combinations per D-03-07. Require legacy-vs-corrected delta fields and no-lookahead metadata/columns in rows per D-03-06. Require an E8/MTES-filtered row to receive a real `mtes_frame` from runner helper code and expose `mtes_conflict_count` plus `mtes_vetoed_entry_count`. Include one subprocess-style smoke command only if it remains under 60 seconds.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_runner.py -q</automated>
  </verify>
  <acceptance_criteria>Runner tests fail until the CLI/report contract exposes required baseline, strategy-combination, cost/slippage, no-lookahead, and metric fields.</acceptance_criteria>
  <done>Experiment runner behavior is specified by automated tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement experiment runner and report generation</name>
  <files>scripts/backtest_supertrend_enhancement.py, agent/tests/test_supertrend_enhancement_runner.py</files>
  <action>Implement `scripts/backtest_supertrend_enhancement.py` following Phase 02 report conventions but importing canonical Phase 03 modules instead of duplicating corrected SuperTrend logic. Load local OHLCV data with the same market/symbol conventions as `scripts/backtest_trend_indicators.py`; support single-symbol and available-universe runs; calculate baselines buy-and-hold, legacy Phase 02 simplified SuperTrend, corrected daily ST, completed weekly ST anchor, daily RangeFilter, and enhanced matrix rows; derive real historical MTES conflict metadata per symbol/bar using `agent/src/analysis/major_trend_evaluator.py` or existing MTES rolling patterns, pass it as `mtes_frame` into `build_enhancement_features`, and report `mtes_conflict_count` plus `mtes_vetoed_entry_count` for MTES-filtered experiments; include a legacy-vs-corrected SuperTrend delta section/columns so algorithm correction is separated from strategy enhancement; simulate trades with explicit next-bar execution semantics or a clearly documented one-bar signal shift; apply configurable trading mode and conservative cost/slippage defaults per D-03-11; call `calculate_phase03_trade_metrics` for metrics; write timestamped CSV and Markdown reports under `reports/` only. Include exclusions in reports when data is insufficient after warmup, rather than silently dropping symbols. Do not add instructions to blindly commit all `reports/` or `scripts/` contents.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_runner.py agent/tests/test_supertrend_calculation.py agent/tests/test_supertrend_enhancement_strategy.py agent/tests/test_supertrend_enhancement_metrics.py -q</automated>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --symbol GC=F --matrix smoke --output reports</automated>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --all --matrix core --max-grid-size 24 --output reports</automated>
  </verify>
  <acceptance_criteria>Smoke run generates one `reports/supertrend_enhancement_comparison_*.csv` and one `reports/supertrend_enhancement_report_*.md`; CSV includes required baselines and at least one enhanced combination; Markdown summarizes baseline comparison, best rows by risk/trade metrics, regime split, no-lookahead statement, cost/slippage assumptions, and insufficient-data exclusions.</acceptance_criteria>
  <done>The runner can produce reproducible Phase 03 research reports for at least a single-symbol smoke run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add matrix and walk-forward phase-gate commands</name>
  <files>scripts/backtest_supertrend_enhancement.py, agent/tests/test_supertrend_enhancement_runner.py</files>
  <action>Add matrix controls that can run E1-E8 core experiments and optional E9/E10 parameter-grid/walk-forward modes with `--max-grid-size` protection to avoid unbounded runtime. Implement walk-forward output fields separating train-selected parameters from test metrics when `--walk-forward` is enabled. Add a runner test that validates grid-size capping and train/test field presence without running a large grid. Provide final verification commands in the plan summary, including a full Phase 03 local-data gate command using `--all --matrix core --max-grid-size 24 --output reports`; the executor must run this bounded full available-universe matrix unless it documents a concrete runtime/data blocker in the summary with the exact reproduction command and subset results.</action>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_enhancement_runner.py -q</automated>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --symbol GC=F --matrix core --walk-forward --max-grid-size 12 --output reports</automated>
  </verify>
  <acceptance_criteria>Runner supports bounded core matrix and walk-forward mode; report rows include train/test or out-of-sample fields when walk-forward is enabled; runtime guard prevents accidental huge parameter grids.</acceptance_criteria>
  <done>Phase 03 can run reproducible bounded experiments and produce comparison reports suitable for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to runner | User-provided symbols, output path, matrix mode, cost/slippage, and grid size control file access and runtime. |
| Local data files to backtest | OHLCV parquet/csv data can be missing, stale, malformed, or insufficient after warmup. |
| Runner to reports | Generated CSV/Markdown reports are written to disk under `reports/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | `--output` path | mitigate | Resolve output path, default to project `reports/`, reject traversal outside project/report-safe paths. |
| T-03-04-02 | Denial of Service | parameter matrix | mitigate | Add `--max-grid-size`, matrix mode limits, and printed/logged matrix size before execution. |
| T-03-04-03 | Integrity | signal/trade simulation | mitigate | Use one-bar shifted/next-bar semantics and include no-lookahead statement in reports. |
| T-03-04-04 | Information Disclosure | generated reports | mitigate | Do not read or print environment variables/secrets; reports contain only strategy metrics and paths. |
| T-03-04-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_supertrend_calculation.py agent/tests/test_supertrend_enhancement_metrics.py agent/tests/test_supertrend_enhancement_strategy.py agent/tests/test_supertrend_validation_plan.py agent/tests/test_supertrend_enhancement_runner.py -q</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --symbol GC=F --matrix smoke --output reports</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --symbol GC=F --matrix core --walk-forward --max-grid-size 12 --output reports</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 scripts/backtest_supertrend_enhancement.py --all --matrix core --max-grid-size 24 --output reports</automated>
</verification>

<success_criteria>
- Runner compares buy-and-hold, legacy Phase 02 SuperTrend, corrected daily ST, weekly ST anchor, daily RF, and enhanced combinations.
- Runner writes CSV/Markdown reports under `reports/` with required Phase 03 metrics, legacy-vs-corrected SuperTrend deltas, regime split, cost/slippage assumptions, no-lookahead statement, and insufficient-data exclusions.
- CLI supports safe bounded smoke/core/walk-forward modes.
- Full Phase 03 verification can run all new tests, a single-symbol smoke, a bounded walk-forward smoke, and a bounded available-universe core matrix using `.venv/bin/python3`.
</success_criteria>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/03-supertrend-enhancement-strategy/03-05-SUMMARY.md` when done.
</output>
