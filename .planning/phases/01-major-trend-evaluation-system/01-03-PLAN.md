---
phase: 01-major-trend-evaluation-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - agent/src/analysis/watchlist_analyzer.py
  - agent/src/analysis/report_generator.py
  - agent/src/tools/watchlist_tool.py
  - agent/tests/test_watchlist_mtes_contract.py
autonomous: true
requirements:
  - MTES-07
  - MTES-08
must_haves:
  truths:
    - Watchlist analysis emits machine-readable MTES fields for every successfully analyzed symbol.
    - Watchlist-specified timeframes are passed into MTES evaluation, and any available higher-timeframe data uses existing completed-bar MTF logic.
    - Human report tables show MTES score/state without corrupting the JSON contract.
  artifacts:
    - path: agent/src/analysis/watchlist_analyzer.py
      provides: Watchlist MTES adapter
    - path: agent/src/analysis/report_generator.py
      provides: Human-readable MTES columns
    - path: agent/src/tools/watchlist_tool.py
      provides: Machine-readable watchlist MTES summary
    - path: agent/tests/test_watchlist_mtes_contract.py
      provides: Watchlist adapter and JSON contract tests
  key_links:
    - from: agent/src/analysis/watchlist_analyzer.py
      to: agent/src/analysis/major_trend_evaluator.py
      via: MajorTrendEvaluator.evaluate
      pattern: MajorTrendEvaluator.*evaluate
---

<objective>
Repair and harden the watchlist integration so existing batch analysis exposes the MTES contract without moving scoring logic into watchlist code.

Purpose: Satisfy SPEC watchlist output requirements and D-13/D-16 while preserving existing trend, pullback, signal, report, and tool behavior.
Output: Thin watchlist adapter, report/table columns, machine-readable tool JSON, and focused contract tests.
</objective>

<context>
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-SPEC.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md
@/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/watchlist_analyzer.py
@/Users/iagent/projects/vibe-trading/agent/src/analysis/report_generator.py
@/Users/iagent/projects/vibe-trading/agent/src/tools/watchlist_tool.py
@/Users/iagent/projects/vibe-trading/agent/src/data/watchlist.py

<interfaces>
Watchlist output contract required for each analyzable row:
- `symbol`, `asset_class`, `trend_score`, `trend_state`, `direction`, `confidence`, `regime`, `sub_scores`, and `top_drivers`.
- Existing `AnalysisResult.to_dict()` may merge `mtes` payload into top-level JSON, but `mtes` storage can remain internal to `AnalysisResult`.
- `WatchlistReader.get_timeframes(symbol)` returns watchlist-configured primary and secondary timeframes; use this for D-13 rather than global defaults.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add watchlist MTES contract tests</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md lines 468-793 for watchlist, report, tool, and timeframe patterns.
    - /Users/iagent/projects/vibe-trading/agent/tests/test_major_trend_evaluator.py for existing watchlist fixture style.
    - /Users/iagent/projects/vibe-trading/agent/src/data/watchlist.py for `get_timeframes` behavior.
  </read_first>
  <files>agent/tests/test_watchlist_mtes_contract.py</files>
  <action>
    Create `agent/tests/test_watchlist_mtes_contract.py` with deterministic fixture tests that write a temporary watchlist row with explicit timeframes, write local primary timeframe OHLCV parquet data, optionally write secondary/higher timeframe OHLCV data if the existing analyzer supports local loading for that timeframe, and assert `WatchlistAnalyzer.analyze_all(verbose=False)[0].to_dict()` contains `symbol`, `asset_class`, `trend_score`, `trend_state`, `direction`, `confidence`, `regime`, `sub_scores`, and `top_drivers`. Add a tool-level JSON test for `analyze_watchlist` or the existing watchlist tool that asserts the `mtes` array contains the same machine-readable fields and no progress text is printed to stdout. Include a test or spy that proves watchlist primary/secondary timeframe names are passed to `MajorTrendEvaluator.evaluate()` per D-13.
  </action>
  <acceptance_criteria>
    Tests fail if MTES fields are absent, nested in a way that breaks the documented JSON contract, not emitted by the watchlist tool, or if watchlist-configured timeframes are ignored.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_watchlist_mtes_contract.py -q</automated>
  </verify>
  <done>Watchlist contract tests prove MTES fields, JSON shape, and watchlist timeframe propagation rules.</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep WatchlistAnalyzer as a thin MTES adapter</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/agent/src/analysis/watchlist_analyzer.py for existing analysis flow.
    - /Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py for evaluator API.
    - /Users/iagent/projects/vibe-trading/agent/src/data/watchlist.py for timeframe parsing.
  </read_first>
  <files>agent/src/analysis/watchlist_analyzer.py</files>
  <action>
    Update `WatchlistAnalyzer` so it calls the core `MajorTrendEvaluator.evaluate()` and merges `result.to_dict()` into `AnalysisResult` output without duplicating scoring formulas. Pass `asset_class` resolved from market, `base_timeframe` from the watchlist primary timeframe, `higher_timeframe_name` from the watchlist secondary timeframe, and available higher-timeframe DataFrame when the analyzer can load it from existing local data paths. If higher-timeframe data is unavailable, call the evaluator without it and let the MTF dimension degrade through evaluator metadata rather than adding a watchlist data-health gate. Per D-18, do not implement or call the deferred local data completeness gate.
  </action>
  <acceptance_criteria>
    `AnalysisResult.to_dict()` exposes all required MTES fields for successful analyses; evaluator failures produce a clear unavailable MTES payload without crashing batch analysis; watchlist local data health checks are not introduced; existing trend/pullback/signal fields remain present.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_watchlist_mtes_contract.py -q</automated>
  </verify>
  <done>WatchlistAnalyzer delegates scoring to the core evaluator and merges MTES output without adding a data-health gate.</done>

</task>

<task type="auto">
  <name>Task 3: Preserve human table and tool JSON surfaces</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/agent/src/analysis/report_generator.py for markdown table generation.
    - /Users/iagent/projects/vibe-trading/agent/src/tools/watchlist_tool.py for JSON response patterns and path-safety helper.
  </read_first>
  <files>agent/src/analysis/report_generator.py, agent/src/tools/watchlist_tool.py</files>
  <action>
    Update `ReportGenerator` only as needed so human-readable tables include MTES score and trend state columns while retaining existing watchlist report structure. Update `watchlist_tool.py` only as needed so the machine-readable summary JSON includes a top-level `mtes` array with `symbol`, `asset_class`, `trend_score`, `trend_state`, `direction`, `confidence`, `regime`, `sub_scores`, and `top_drivers`. Keep existing `_resolve_watchlist_path()` path traversal protection unchanged and ensure JSON values are plain strings/numbers/lists/dicts suitable for machine consumers.
  </action>
  <acceptance_criteria>
    Markdown reports show MTES fields for humans; JSON tool output contains stable plain MTES fields; path safety remains enforced; no report-only formatting changes leak into machine fields.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_watchlist_mtes_contract.py agent/tests/test_strategy_watchlist_tools.py -q</automated>
  </verify>
  <done>Human tables and watchlist tool JSON expose stable MTES fields while preserving path safety and existing watchlist behavior.</done>

</task>

</tasks>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_watchlist_mtes_contract.py -q</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py agent/tests/test_watchlist_mtes_contract.py agent/tests/test_strategy_watchlist_tools.py -q</automated>
Note: the existing full `agent/tests -q` collection error in loader cache tests is outside MTES-focused acceptance for this plan; focused watchlist and MTES tests must pass.
</verification>

<success_criteria>
- Watchlist JSON contract satisfies MTES-08 for every successfully analyzed fixture row.
- Watchlist timeframes drive MTES base/higher timeframe parameters per D-13.
- MTF safety remains delegated to the evaluator and `MTFAligner` per D-14.
- Deferred watchlist local data health gate is not implemented or folded into MTES per D-18.
</success_criteria>

<must_haves>
- Observable truth: A watchlist analysis result includes MTES score, state, direction, confidence, regime, sub-scores, and drivers.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/src/analysis/watchlist_analyzer.py` remains an adapter that calls the core evaluator.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/src/tools/watchlist_tool.py` returns machine-readable MTES JSON.
- Key link: watchlist timeframes from `WatchlistReader` are passed to `MajorTrendEvaluator.evaluate()`.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watchlist CSV/tool input to analyzer | Symbols, markets, and timeframe strings from watchlist files influence data loading and evaluator parameters. |
| Watchlist path to filesystem | Tool users can supply watchlist paths that must stay inside the watchlist directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | timeframe strings | mitigate | Reuse `WatchlistReader.get_timeframes()` and pass names as evaluator metadata; evaluator validates scoring inputs. |
| T-01-07 | Elevation of Privilege | `_resolve_watchlist_path()` | mitigate | Preserve existing resolved-path containment under watchlist root and add no bypasses. |
| T-01-08 | Denial of Service | batch analyzer per-symbol MTES failure | mitigate | Catch evaluator exceptions per symbol and emit unavailable MTES payload without aborting entire batch. |
| T-01-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-03-SUMMARY.md` when done.
</output>
