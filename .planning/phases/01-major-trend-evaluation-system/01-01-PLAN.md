---
phase: 01-major-trend-evaluation-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/analysis/major_trend_evaluator.py
  - agent/tests/test_major_trend_evaluator.py
autonomous: true
requirements:
  - MTES-01
  - MTES-02
  - MTES-03
  - MTES-04
  - MTES-05
  - MTES-06
  - MTES-07
must_haves:
  truths:
    - Core MTES evaluator returns a deterministic 0-100 quality score only when long-horizon data is sufficient.
    - Direction sign is emitted independently from trend_score and drives bull/bear state side.
    - The evaluator exposes six locked dimension sub-scores and Base+Override asset profiles for stock, etf, futures, crypto, and fx.
    - MTF scoring uses completed higher-timeframe bars through MTFAligner and surfaces conflicts without vetoing total score.
  artifacts:
    - path: agent/src/analysis/major_trend_evaluator.py
      provides: Core reusable MTES evaluator, result schema, asset profiles, scoring dimensions, MTF scoring
    - path: agent/tests/test_major_trend_evaluator.py
      provides: Focused evaluator fixtures and contract tests
  key_links:
    - from: agent/src/analysis/major_trend_evaluator.py
      to: agent/backtest/strategies/mtf.py
      via: MTFAligner(MTFConfig(lag_bars=1)).align_htf_to_ltf
      pattern: MTFAligner.*align_htf_to_ltf
---

<objective>
Align the existing draft MTES core evaluator with the locked Phase 01 scoring decisions and add focused tests that make those decisions executable.

Purpose: Establish the reusable scoring contract consumed by both the backtest wrapper and watchlist adapter without duplicating indicator logic.
Output: Spec-compliant `MajorTrendEvaluator` plus focused pytest coverage for scoring, classification, profiles, insufficient data, momentum/regime metadata, and MTF safety.
</objective>

<context>
@/Users/iagent/projects/vibe-trading/.planning/ROADMAP.md
@/Users/iagent/projects/vibe-trading/.planning/STATE.md
@/Users/iagent/projects/vibe-trading/.planning/REQUIREMENTS.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-SPEC.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md
@/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py
@/Users/iagent/projects/vibe-trading/agent/tests/test_major_trend_evaluator.py
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/mtf.py

<interfaces>
Core evaluator target contract from research and patterns:
- `MajorTrendEvaluator(config).evaluate(df, asset_class, higher_timeframe=None, base_timeframe="1d", higher_timeframe_name="1w", cross_section_context=None) -> MajorTrendResult`.
- `MajorTrendResult.to_dict()` must include `asset_class`, `trend_score`, `trend_state`, `direction`, `confidence`, `regime`, `sub_scores`, `raw_scores`, `weights`, `top_drivers`, `regime_flags`, `explanation`, and `metadata`.
- Locked dimensions are `direction`, `strength`, `structure`, `momentum`, `volatility_regime`, and `mtf`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock evaluator contract tests before refactor</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-CONTEXT.md lines 42-68 for D-01 through D-18.
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-RESEARCH.md lines 263-292 for requirement-to-test gaps.
    - /Users/iagent/projects/vibe-trading/agent/tests/test_major_trend_evaluator.py for existing fixture style.
  </read_first>
  <files>agent/tests/test_major_trend_evaluator.py</files>
  <action>
    Extend `agent/tests/test_major_trend_evaluator.py` with focused failing tests for the locked core behaviors: Base+Override profile composition using base weights direction 15, strength 15, structure 25, momentum 15, volatility_regime 15, mtf 15 per D-01 and D-04; all composed profiles for stock, etf, futures, crypto, and fx total 100; asset-class-specific direction periods are observable per D-06; a frame shorter than the resolved long-horizon direction requirement returns `regime_flags == ["insufficient_data"]`, `trend_score == 0.0`, and metadata status indicating no score per D-07; cross-sectional momentum context changes or annotates relative momentum while single-asset analysis remains absolute per D-10; MTF conflict strongly lowers only the MTF raw/sub-score and surfaces `timeframe_conflict` in metadata or top drivers per D-15. Keep existing tests and fixture helpers; add only MTES-focused tests.
  </action>
  <acceptance_criteria>
    Tests explicitly fail against any evaluator that uses full per-asset profiles without the locked base profile, scores long-horizon-insufficient data, reuses one global direction period for all assets, omits relative momentum context metadata, or treats MTF conflict as a hard veto.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py -q</automated>
  </verify>
  <done>Focused evaluator contract tests are added and codify the locked MTES behaviors that should fail until the evaluator implements them.</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor profile, score, direction, and classification internals</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py for existing draft evaluator.
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md lines 24-203 for evaluator patterns and required deviations.
  </read_first>
  <files>agent/src/analysis/major_trend_evaluator.py</files>
  <action>
    Update `agent/src/analysis/major_trend_evaluator.py` so the evaluator implements locked Base+Override profiles per D-01, uses the structure-first base weights exactly per D-04, applies the D-02 weighted-sum-plus-limited-penalties composition for `trend_score`, keeps `trend_score` as a weighted quality score independent from `direction` per D-03, and classifies seven states with fixed thresholds where direction determines bull/bear side and score bands determine early/confirmed/strong per D-08. Implement the D-05 MA+Slope+Return primary direction framework with asset-class-specific direction period configuration per D-06 and require the resolved long-window data length before scoring direction; if unavailable, return the existing no-score insufficient result per D-07 instead of using shorter proxies or reallocating weights. Preserve deterministic output and `MajorTrendResult.to_dict()` fields.
  </action>
  <acceptance_criteria>
    `BASE_WEIGHTS` or equivalent exposes the exact locked structure-first profile; `get_weight_profile()` composes supported asset profiles from base plus overrides and rejects unsupported classes with a clear `ValueError`; `trend_score` stays within 0-100 and equals the rounded sum of six sub-scores; direction is one of BULL, BEAR, NEUTRAL and is not encoded as the score sign; insufficient long-horizon data returns no score with explicit metadata.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py -q</automated>
  </verify>
  <done>The evaluator returns a valid MTES result with the locked base profile, D-02 weighted-sum-plus-limited-penalties score composition, D-05 MA+Slope+Return direction framework, and insufficient-data no-score behavior.</done>

</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete dimension metadata, relative momentum, regime, and MTF conflict handling</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-CONTEXT.md lines 54-64 for D-09 through D-15.
    - /Users/iagent/projects/vibe-trading/agent/backtest/strategies/mtf.py for MTFAligner completed-bar lag behavior.
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-RESEARCH.md lines 184-193 for dimension guidance.
  </read_first>
  <files>agent/src/analysis/major_trend_evaluator.py, agent/tests/test_major_trend_evaluator.py</files>
  <action>
    Ensure structure combines breakout or range position with swing higher-high/higher-low or lower-low/lower-high logic per D-09; momentum combines absolute 3M, 6M, and 12M returns and optional relative ranking when `cross_section_context` exists per D-10; regime primarily penalizes low trend efficiency and chop, while ATR/HV extremes are metadata flags or secondary penalties per D-11; unavailable non-core sub-indicators degrade only their own sub-score and mark metadata without reallocating weight per D-12. Keep all MTF scoring routed through `MTFAligner(MTFConfig(lag_bars=1)).align_htf_to_ltf` per D-14. For base/higher conflicts, reduce the MTF raw score and add `timeframe_conflict` to metadata or top drivers without forcing `trend_score` to zero per D-15.
  </action>
  <acceptance_criteria>
    Structure metadata reports both breakout/range and swing evidence; momentum metadata reports absolute windows and relative-rank status when context is provided; choppy fixtures score lower in regime than smooth directional fixtures and extreme ATR/HV appear as flags; MTF metadata includes the aligner method and conflict state; focused tests prove no hard veto occurs on MTF conflict.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py -q</automated>
  </verify>
  <done>Structure, momentum, regime, and MTF metadata are emitted consistently, and MTF conflicts reduce the MTF sub-score without hard-vetoing the total trend score.</done>

</task>

</tasks>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py -q</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py::test_asset_profiles_total_100 agent/tests/test_major_trend_evaluator.py::test_insufficient_data_returns_warning_without_crashing -q</automated>
Note: the existing full `agent/tests -q` collection error in loader cache tests is outside MTES-focused acceptance for this plan; do not mark this plan failed solely because of that unrelated collection error.
</verification>

<success_criteria>
- The core evaluator satisfies MTES-01 through MTES-07 from the SPEC.
- Every locked decision D-01 through D-15 has executable evaluator behavior or focused test coverage.
- Focused evaluator tests pass using `.venv/bin/python3 -m pytest`.
- No watchlist local data health gate work is added; D-18 remains out of scope.
</success_criteria>

<must_haves>
- Observable truth: MTES can score a sufficient OHLCV fixture into six visible dimensions and a seven-state trend label.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py` implements the reusable evaluator.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/tests/test_major_trend_evaluator.py` proves profile, classification, insufficient-data, MTF, regime, and momentum behavior.
- Key link: evaluator MTF logic imports and uses `MTFAligner` instead of a custom higher-to-lower timeframe merge.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame input to evaluator | Untrusted OHLCV columns, date indexes, asset classes, and optional cross-sectional context enter scoring logic. |
| Evaluator to MTFAligner | Higher-timeframe data must not leak incomplete future bars into lower-timeframe decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `normalize_ohlcv` / evaluator inputs | mitigate | Validate required OHLC columns, coerce numeric columns, sort index, and reject unsupported asset classes. |
| T-01-02 | Information Disclosure | MTES result metadata | accept | Metadata contains local scoring diagnostics, no secrets or PII. |
| T-01-03 | Tampering | MTF scoring | mitigate | Use `MTFAligner` with `lag_bars=1` and focused no-look-ahead tests. |
| T-01-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-01-SUMMARY.md` when done.
</output>
