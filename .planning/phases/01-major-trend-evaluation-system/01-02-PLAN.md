---
phase: 01-major-trend-evaluation-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - agent/backtest/strategies/major_trend.py
  - agent/backtest/strategies/__init__.py
  - agent/backtest/strategies/registry.py
  - agent/tests/test_mtes_strategy.py
autonomous: true
requirements:
  - MTES-01
  - MTES-02
  - MTES-03
  - MTES-09
  - D-16
  - D-17
must_haves:
  truths:
    - D-16: Expose dual adapters around core evaluator, including a backtest strategy wrapper.
    - D-17: Keep backtest wrapper evaluation-only (no live execution/position sizing).
    - Backtests can consume MTES as an evaluation strategy without live execution or position sizing.
    - The strategy registry exposes the MTES wrapper alongside existing trend baselines.
    - Generated backtest output includes MTES score/state/direction columns and deterministic directional evaluation signals.
  artifacts:
    - path: agent/backtest/strategies/major_trend.py
      provides: MTES backtest strategy wrapper
    - path: agent/tests/test_mtes_strategy.py
      provides: Strategy wrapper and registration tests
  key_links:
    - from: agent/backtest/strategies/major_trend.py
      to: agent/src/analysis/major_trend_evaluator.py
      via: MajorTrendEvaluator.evaluate
      pattern: MajorTrendEvaluator.*evaluate
---

<objective>
Add the MTES backtest strategy wrapper and register it in the existing strategy system.

Purpose: Provide the preferred validation path required by D-16 while keeping the wrapper evaluation-only per D-17.
Output: A registered `MajorTrendEvaluationStrategy` or equivalently named strategy that maps MTES states to evaluation signals and exposes MTES columns for comparison against baselines.
</objective>

<context>
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-SPEC.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-CONTEXT.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-RESEARCH.md
@/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md
@/Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/__init__.py
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/registry.py
@/Users/iagent/projects/vibe-trading/agent/backtest/strategies/trend.py
@/Users/iagent/projects/vibe-trading/agent/tests/test_strategy_watchlist_tools.py

<interfaces>
Existing strategy interface:
- Strategies subclass `BaseStrategy` from `agent/backtest/strategies/__init__.py`.
- `BaseStrategy.generate(df)` validates OHLC data, calls `_calculate(df)`, calls `_generate_signals(df, indicators)`, and returns the source DataFrame plus `signal`, `signal_name`, and indicator columns.
- Registration happens in `agent/backtest/strategies/registry.py` by instantiating strategies and calling `StrategyRegistry.register(strategy)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strategy wrapper tests</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-PATTERNS.md lines 206-464 for strategy and registry patterns.
    - /Users/iagent/projects/vibe-trading/agent/tests/test_strategy_watchlist_tools.py for registry/tool test style.
    - /Users/iagent/projects/vibe-trading/agent/backtest/strategies/__init__.py for BaseStrategy behavior.
  </read_first>
  <files>agent/tests/test_mtes_strategy.py</files>
  <action>
    Create `agent/tests/test_mtes_strategy.py` with tests that prove the MTES strategy is registered under a stable name such as `major_trend_evaluation`, appears when listing trend strategies, generates a DataFrame containing `signal`, `signal_name`, `mtes_score`, `mtes_state`, `mtes_direction`, `mtes_regime`, and six MTES sub-score columns, and maps confirmed or strong bull states to `1`, confirmed or strong bear states to `-1`, and early/choppy/insufficient states to `0`. Include an explicit assertion that the strategy output does not include position size, order quantity, portfolio allocation, or live execution fields per D-17.
  </action>
  <acceptance_criteria>
    Tests fail before the wrapper and registration exist, and tests encode D-16 and D-17 without touching unrelated strategy families.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_mtes_strategy.py -q</automated>
  </verify>
  <done>Wrapper and registration tests exist and encode D-16/D-17 expectations for evaluation-only MTES strategy output.</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evaluation-only MTES strategy wrapper</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/agent/backtest/strategies/trend.py for strategy metadata, tags, supported markets, `_calculate`, and `_generate_signals` style.
    - /Users/iagent/projects/vibe-trading/agent/src/analysis/major_trend_evaluator.py for evaluator API and state labels.
  </read_first>
  <files>agent/backtest/strategies/major_trend.py</files>
  <action>
    Create `agent/backtest/strategies/major_trend.py` with a `MajorTrendEvaluationStrategy` that subclasses `BaseStrategy`, uses `StrategyType.TREND`, declares supported markets spanning stocks, ETFs, futures, crypto, and FX where consistent with existing project market labels, calls `MajorTrendEvaluator.evaluate()` in an evaluation-oriented rolling or full-frame manner compatible with `BaseStrategy.generate()`, and emits MTES indicator series/columns. Map `BULL_CONFIRMED` and `BULL_STRONG` to signal `1`, `BEAR_CONFIRMED` and `BEAR_STRONG` to signal `-1`, and all other states to `0`. Do not add live trading, order execution, position sizing, portfolio allocation, or parameter-mining logic.
  </action>
  <acceptance_criteria>
    Strategy generation works on deterministic OHLCV fixtures; output has `signal_name == "major_trend_evaluation"` or the chosen stable strategy name; MTES metadata columns are present; the wrapper remains a backtest/evaluation strategy only.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_mtes_strategy.py -q</automated>
  </verify>
  <done>The MTES strategy wrapper subclasses BaseStrategy, emits MTES columns, and maps confirmed/strong bull and bear states to evaluation signals only.</done>

</task>

<task type="auto">
  <name>Task 3: Register and export MTES strategy</name>
  <read_first>
    - /Users/iagent/projects/vibe-trading/agent/backtest/strategies/registry.py for auto-registration pattern.
    - /Users/iagent/projects/vibe-trading/agent/backtest/strategies/__init__.py for export/import pattern.
  </read_first>
  <files>agent/backtest/strategies/registry.py, agent/backtest/strategies/__init__.py</files>
  <action>
    Update `agent/backtest/strategies/registry.py` to import and auto-register `MajorTrendEvaluationStrategy` in the trend strategy block, and update `agent/backtest/strategies/__init__.py` to export the wrapper without changing the `BaseStrategy.generate()` contract. Keep existing `TrendEmaAdxStrategy`, `TrendMacdStrategy`, and `TrendDualEmaStrategy` available as baselines for the validation plan.
  </action>
  <acceptance_criteria>
    `StrategyRegistry.get("major_trend_evaluation")` or the chosen stable name returns the wrapper instance after registry import; existing trend strategy names still register; existing strategy tool tests continue to pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_mtes_strategy.py agent/tests/test_strategy_watchlist_tools.py -q</automated>
  </verify>
  <done>The MTES strategy is auto-registered and exported while existing trend baselines remain available.</done>

</task>

</tasks>

<verification>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_mtes_strategy.py -q</automated>
<automated>cd /Users/iagent/projects/vibe-trading && .venv/bin/python3 -m pytest agent/tests/test_major_trend_evaluator.py agent/tests/test_mtes_strategy.py agent/tests/test_strategy_watchlist_tools.py -q</automated>
Note: the existing full `agent/tests -q` collection error in loader cache tests is outside MTES-focused acceptance for this plan; focused MTES and strategy tests must pass.
</verification>

<success_criteria>
- MTES is available as a reusable backtest strategy wrapper per D-16.
- The wrapper is evaluation-only and excludes live execution and position sizing per D-17.
- Existing trend baselines remain registered for later validation-plan comparisons.
- Focused strategy and registry tests pass.
</success_criteria>

<must_haves>
- Observable truth: Backtest code can generate MTES evaluation signals from OHLCV input.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/backtest/strategies/major_trend.py` exists and subclasses `BaseStrategy`.
- Required artifact: `/Users/iagent/projects/vibe-trading/agent/tests/test_mtes_strategy.py` proves wrapper output and registration.
- Key link: strategy wrapper calls the core evaluator instead of reimplementing MTES scoring.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backtest data to strategy | OHLCV DataFrame enters strategy wrapper and evaluator. |
| Strategy registry to callers | Registered strategy metadata becomes discoverable by tools/backtest runners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `MajorTrendEvaluationStrategy.generate()` | mitigate | Rely on `BaseStrategy.validate()` OHLC validation and evaluator validation. |
| T-01-05 | Elevation of Privilege | Strategy wrapper scope | mitigate | Do not add execution, order, sizing, or allocation fields; tests assert evaluation-only output. |
| T-01-SC | Tampering | package installs | mitigate | No package-manager install tasks are planned. |
</threat_model>

<output>
Create `/Users/iagent/projects/vibe-trading/.planning/phases/01-major-trend-evaluation-system/01-02-SUMMARY.md` when done.
</output>
