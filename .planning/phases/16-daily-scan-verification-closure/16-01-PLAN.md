---
phase: "16"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "agent/tests/test_scan_verification.py"
autonomous: true
requirements:
  - "TST-01"
must_haves:
  truths:
    - "User can run focused tests showing data-health FAIL blocks strategy analysis"
    - "User can run focused tests showing data-health WARN continues with visible caveats"
    - "User can run focused tests showing each symbol assigned to exactly one bucket"
    - "User can run focused tests showing artifact schemas and counts stay consistent"
    - "User can run focused tests showing unsafe paths rejected and CLI exit semantics stable"
  artifacts:
    - path: "agent/tests/test_scan_verification.py"
      provides: "TST-01 focused tests covering gate, buckets, artifacts, path safety, CLI semantics"
      min_lines: 150
---

<objective>
Implement focused verification tests for the daily scan pipeline (TST-01). Cover: gate FAIL/WARN behavior, bucket invariant, artifact schema/consistency, path safety, and CLI exit semantics across success/warning/blocked/failure cases.

Purpose: Fulfills TST-01 so the user can verify the entire scan pipeline end-to-end.
Output: test_scan_verification.py with all focused tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@agent/tests/test_scan_gate.py
@agent/tests/test_scan_signal_buckets.py
@agent/tests/test_scan_reporting.py
@agent/cli/commands/scan.py

# Key patterns from existing test files:

From test_scan_gate.py:
- Uses `CliRunner` from `click.testing`
- Creates temp dir, temp watchlist CSV, temp parquet files
- Uses `_make_ohlcv_df()` helper for test data
- Tests gate FAIL/WARN/PASS by manipulating parquet timestamps

From test_scan_signal_buckets.py:
- Tests bucket classification via `classify_trading_signal()`
- Tests exactly-one-bucket invariant via parameterized tests
- Tests graceful degradation (failed symbol does not abort scan)
- Tests scan_results.json schema

From test_scan_reporting.py:
- Tests Manifest schema and JSON roundtrip
- Tests MarkdownReportRenderer sections and trading-advice avoidance
- Uses `_make_mock_signal_report()` and `_make_mock_health_report()` fixtures

# Test scan.py CLI via CliRunner:
```python
from click.testing import CliRunner
runner = CliRunner()
result = runner.invoke(scan, ['--help'])  # smoke test
result = runner.invoke(scan, ['--watchlist', '/tmp/test.csv', '--run'])
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_scan_verification.py</name>
  <files>agent/tests/test_scan_verification.py</files>
  <read_first>
    agent/tests/test_scan_gate.py
    agent/tests/test_scan_signal_buckets.py
    agent/tests/test_scan_reporting.py
  </read_first>
  <action>
    Create `agent/tests/test_scan_verification.py` with comprehensive TST-01 verification tests. Follow the existing test patterns from the read_first files.

    **Class `TestGateBlocksStrategy` (TST-01: FAIL blocks analysis)**:
    - `test_gate_fail_blocks_signal_scan`: Use CliRunner to invoke scan with a watchlist whose data is stale/empty (causes gate FAIL). Assert exit code != 0 AND signal results file is NOT written. Create temp watchlist CSV pointing to a corrupt parquet so the gate fails.
    - `test_gate_fail_writes_health_json`: Same as above, assert `data_health.json` IS written even on FAIL.
    - `test_gate_fail_writes_manifest`: Same as above, assert `manifest.json` IS written even on FAIL.
    - `test_gate_fail_does_not_write_report_md`: Assert `report.md` is NOT written on FAIL.

    **Class `TestGateWarnsContinues` (TST-01: WARN continues)**:
    - `test_gate_warn_continues_with_caveats`: Use CliRunner with a watchlist whose data triggers WARN (e.g., some symbols fresh, some stale). Assert exit code == 0 AND caveats are visible in output.
    - `test_gate_warn_writes_all_artifacts`: Same as above, assert all 4 artifacts are written.

    **Class `TestBucketInvariant` (TST-01: exactly one bucket per symbol)**:
    - `test_every_symbol_in_exactly_one_bucket`: Run signal scan with a mix of valid/invalid/missing symbols. Load scan_results.json. For each symbol in the results, assert it appears in exactly one of the 5 bucket lists. (Same test concept as test_scan_signal_buckets.py but exercises the full run_signal_scan pipeline.)
    - `test_total_equals_count_of_all_buckets`: From scan_results.json, assert total == sum of all 5 bucket counts.

    **Class `TestArtifactConsistency` (TST-01: schema and counts)**:
    - `test_scan_results_json_schema`: Assert scan_results.json has keys: scan_info, buckets_summary, buckets, metadata. Assert buckets_summary keys: actionable, watch, risk_excluded, skipped, failed, total.
    - `test_data_health_json_schema`: Assert data_health.json has keys: status, total_checks, blocking_failures, warnings.
    - `test_manifest_json_schema`: Assert manifest.json has keys: scan_date, watchlist_name, version, artifacts, scan_info, total_symbols. Assert artifacts has keys: data_health, scan_results, report.
    - `test_report_md_sections_match_artifacts`: Read report.md, assert it contains sections for each non-empty bucket from scan_results.json.
    - `test_bucket_counts_match_report`: Parse report.md for counts, compare to scan_results.json buckets_summary.

    **Class `TestPathSafety` (TST-01: unsafe paths rejected)**:
    - `test_scan_with_nonexistent_watchlist_fails`: Use CliRunner with a non-existent watchlist path. Assert exit code != 0.
    - `test_scan_with_absolute_output_path`: Use CliRunner with `--output /tmp/scan-test-<random>` to verify absolute paths are accepted.
    - `test_scan_with_relative_output_path`: Use CliRunner with `--output scan-test-output` to verify relative paths work.
    - `test_scan_data_dir_not_exists_warns`: Use CliRunner with a non-existent `--data-dir`. Assert scan either warns or fails gracefully (exit code != 0 or warning in output).

    **Class `TestCliExitSemantics` (TST-01: stable exit codes)**:
    - `test_cli_exit_code_0_on_success`: Run scan with valid watchlist, fresh data, --run. Assert exit_code == 0.
    - `test_cli_exit_code_1_on_validation_failure`: Run scan with malformed CSV. Assert exit_code == 1.
    - `test_cli_exit_code_1_on_gate_fail`: Run scan with stale data causing gate FAIL. Assert exit_code == 1.
    - `test_cli_exit_code_0_on_plan_mode`: Run scan without --run. Assert exit_code == 0 (plan mode never fails on its own).
    - `test_cli_help_exits_0`: Run scan --help. Assert exit_code == 0.

    **Helper fixtures** (re-use or adapt from existing test files):
    - `_make_ohlcv_df(n_rows=100)` — same as in test_scan_signal_buckets.py
    - `_make_temp_parquet(df, tmp_path, market, symbol)` — write df to tmp_path/market/symbol/1d.parquet
    - `_make_temp_watchlist(tmp_path, symbols)` — write watchlist CSV
    - `_make_full_scan(tmp_dir, watchlist_path, data_dir)` — invoke scan via CliRunner with --run

    For the full scan tests, use `CliRunner(mix_stderr=False)` to capture both stdout and stderr.
  </action>
  <acceptance_criteria>
    - [ ] Gate FAIL tests: exit_code != 0, data_health.json written, manifest.json written, report.md NOT written
    - [ ] Gate WARN tests: exit_code == 0, all 4 artifacts written, caveats visible
    - [ ] Bucket invariant tests pass (exactly one bucket per symbol)
    - [ ] All 4 artifact schema tests pass
    - [ ] Path safety tests pass (invalid paths rejected gracefully)
    - [ ] CLI exit code tests: 0 on success/plan/warn, 1 on validation failure/gate fail
    - [ ] Tests run with `cd agent && .venv/bin/python -m pytest tests/test_scan_verification.py -x -q`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/iagent/projects/vibe-trading/agent && .venv/bin/python -m pytest tests/test_scan_verification.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>test_scan_verification.py exists with all TST-01 tests and passes pytest -x -q.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI exit codes | Verified via CliRunner across success/failure/warn cases |
| Artifact schemas | Verified via JSON schema assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | — | test_scan_verification.py | N/A | Verification-only module; no production code changes |
</threat_model>

<verification>
- TST-01 behaviors verified by TestGateBlocksStrategy, TestGateWarnsContinues, TestBucketInvariant, TestArtifactConsistency, TestPathSafety, TestCliExitSemantics
</verification>

<success_criteria>
1. `cd agent && .venv/bin/python -m pytest tests/test_scan_verification.py -x -q` passes with all tests green
2. All scan pipeline tests (gate + signal + reporting + verification) pass: `cd agent && .venv/bin/python -m pytest tests/test_scan_gate.py tests/test_scan_signal_buckets.py tests/test_scan_reporting.py tests/test_scan_verification.py -x -q`
3. No new import errors introduced
</success_criteria>

<output>
Create `.planning/phases/16-daily-scan-verification-closure/16-01-SUMMARY.md` when done
</output>
