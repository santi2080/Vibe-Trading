---
phase: 10-close-gap-data-rpt-empirical-composite-backtest-validation
plan: 3
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/comparison_report.md
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/mtes_supertrend/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/mtesv3_only/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/supertrend_only/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-4h.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - METR-01
  - METR-02
  - METR-03
  - RPT-01
  - RPT-03
must_haves:
  truths:
    - "4H support is attempted with the same fixed strategy variants and 2024-2026 date range"
    - "If 4H is unavailable, the limitation is documented as partial/blocked evidence, not omitted"
    - "4H metrics are comparable when verified and explicitly marked unavailable when blocked"
  artifacts:
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/comparison_report.md"
      provides: "4H composite-vs-single comparison output when available"
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-4h.json"
      provides: "Machine-readable 4H verified/partial/blocked evidence"
  key_links:
    - from: "artifacts/configs/composite-empirical-4h.yaml"
      to: "agent/backtest/composite_backtest_compare.py"
      via: "--config"
      pattern: "interval.*4H"
    - from: "data readiness artifacts"
      to: "evidence-4h.json"
      via: "blocked/partial reason propagation"
      pattern: "blocked|partial|verified"
---

<objective>
Attempt the 4H empirical composite-vs-single comparison and record verified, partial, or blocked evidence.

Purpose: DATA-03 requires 1D and 4H support. This plan proves 4H was attempted and captures comparable evidence when available, or exact limitations when unavailable.
Output: 4H comparison artifacts when possible plus `evidence-4h.json` in all cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-composite-strategy-backtest/09-SUMMARY.md
@.planning/phases/09-composite-strategy-backtest/09-SECURITY.md
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json
@agent/backtest/composite_backtest_compare.py
@agent/backtest/reporting/composite_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt 4H composite-vs-single empirical comparison</name>
  <read_first>
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json
agent/backtest/composite_backtest_compare.py
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/comparison_report.md, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/mtes_supertrend/run_card.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/mtesv3_only/run_card.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/supertrend_only/run_card.json</files>
  <action>Attempt the 4H command recorded in `empirical-run-manifest.json`, invoking `python -m backtest.composite_backtest_compare --config .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml --run-root .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h --timeout-seconds 300`. If data-health artifacts already prove 4H is unavailable for all attempted symbols, do not substitute another timeframe; create `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/run-status.json` with status `blocked` and source data-health reasons. If only some symbols are eligible, use the config generated by Plan 10-01 and preserve partial coverage in evidence. For attempted runs, capture process exit code and bounded stdout/stderr diagnostics into `run-status.json` whether the command succeeds or fails. Keep Phase 09 controls enabled: safe run root, variant allowlist, safe YAML/JSON loading, trusted signal-engine verification, subprocess timeout/redaction, CSV safety, and row/artifact limits.</action>
  <verify>
    <automated>PY=.venv/bin/python; [ -x "$PY" ] || PY=python; "$PY" - <<'PY'
import json, os, subprocess, sys
from pathlib import Path
root = Path('.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts')
run_root = root / 'runs' / '4h'
run_root.mkdir(parents=True, exist_ok=True)
cmd = [
    sys.executable, '-m', 'backtest.composite_backtest_compare',
    '--config', str(root / 'configs' / 'composite-empirical-4h.yaml'),
    '--run-root', str(run_root),
    '--timeout-seconds', '300',
]
env = dict(os.environ)
env['PYTHONPATH'] = 'agent'
proc = subprocess.run(cmd, env=env, text=True, capture_output=True, timeout=360)
status = {
    'timeframe': '4H',
    'command': cmd,
    'exit_code': proc.returncode,
    'status': 'succeeded' if proc.returncode == 0 else 'partial_or_blocked',
    'stdout_tail': proc.stdout[-4000:],
    'stderr_tail': proc.stderr[-4000:],
}
(run_root / 'run-status.json').write_text(json.dumps(status, indent=2), encoding='utf-8')
if proc.returncode == 0:
    assert (run_root / 'comparison_report.md').exists(), 'missing comparison_report.md after successful run'
else:
    assert (run_root / 'run-status.json').exists(), 'missing run-status.json for failed/blocked run'
print('4h run status captured:', status['status'])
PY</automated>
  </verify>
  <acceptance_criteria>The 4H comparison is attempted exactly once from the fixed 4H config unless pre-run data-health evidence proves a full block. Successful runs produce comparison and run-card artifacts; failed/blocked runs retain bounded diagnostics for Task 2.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory 4H verified/partial/blocked evidence</name>
  <read_first>
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/4h/comparison_report.md
.planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-4h.json</files>
  <action>Create `evidence-4h.json` with the same schema as `evidence-1d.json`: `timeframe`, `status`, `config_path`, `run_root`, `variants`, normalized metrics, artifact presence, symbols attempted, excluded symbols, and limitations. Required status semantics: `verified` only if all three variants have run cards and required metrics; `partial` if at least one symbol/variant/timeframe produced evidence but coverage is incomplete; `blocked` if no 4H empirical run could execute because data availability or loader support blocked it. Include `blockers` with source artifact references and exact messages. Do not mark DATA-03 as verified unless both the attempted 4H config and empirical evidence or explicit partial/blocked record exist.</action>
  <verify>
    <automated>.venv/bin/python - <<'PY'
import json
from pathlib import Path
path = Path('.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-4h.json')
evidence = json.loads(path.read_text())
assert evidence['timeframe'] == '4H'
assert evidence['status'] in {'verified', 'partial', 'blocked'}
assert 'config_path' in evidence and 'run_root' in evidence
if evidence['status'] == 'verified':
    assert set(evidence['variants']) == {'MTES+SuperTrend', 'MTESv3-only', 'SuperTrend-only'}
    required = {'total_return', 'win_rate', 'sharpe_ratio', 'max_drawdown', 'trade_count'}
    for variant, payload in evidence['variants'].items():
        assert required <= set(payload['metrics']), (variant, payload.get('metrics'))
else:
    assert evidence.get('limitations') or evidence.get('blockers'), 'partial/blocked 4H must explain why'
print('evidence-4h.json: PASS')
PY</automated>
  </verify>
  <acceptance_criteria>`evidence-4h.json` is valid, uses the same metric schema as 1D when verified, and explicitly records partial/blocked status with source references when 4H evidence is unavailable.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4H config to subprocess | The attempted 4H config drives empirical subprocess runs. |
| Data-availability boundary | Missing/partial 4H data must cross into evidence as limitations, not scope reduction. |
| Artifact boundary | 4H evidence and diagnostics are serialized to `.planning` artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-T1 | Tampering | 4H config/status | mitigate | Do not rewrite 4H to 1D or another interval; status must reflect attempted 4H evidence. |
| T-10-03-R1 | Repudiation | Blocked/partial claims | mitigate | Record source artifact paths and exact blocker messages in `evidence-4h.json`. |
| T-10-03-I1 | Information Disclosure | Failure diagnostics | mitigate | Preserve Phase 09 subprocess output redaction/truncation; no secrets in JSON. |
| T-10-03-D1 | Denial of Service | Intraday empirical run | mitigate | Use timeout and row/artifact limits; do not disable caps. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
- Attempt the 4H comparison command or document pre-run full block from readiness evidence.
- Parse `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-4h.json` and verify status semantics.
</verification>

<success_criteria>
- DATA-03 has explicit 4H attempted evidence.
- 4H verified metrics are captured when available.
- 4H partial/blocked status is documented with exact limitations when unavailable.
- RPT-03 has timeframe-specific data-quality status for final reporting.
</success_criteria>

<output>
Create `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/10-03-SUMMARY.md` when done.
</output>
