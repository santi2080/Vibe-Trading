---
phase: 10-close-gap-data-rpt-empirical-composite-backtest-validation
plan: 2
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/comparison_report.md
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/mtes_supertrend/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/mtesv3_only/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/supertrend_only/run_card.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-1d.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - METR-01
  - METR-02
  - METR-03
  - RPT-01
must_haves:
  truths:
    - "Composite, MTES-only, and SuperTrend-only 1D variants are run from the same 2024-2026 config"
    - "Comparable metrics exist for return, win rate, Sharpe, max drawdown, and trade count"
    - "Per-source signal artifacts are captured where Phase 09 infrastructure emits them"
  artifacts:
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/comparison_report.md"
      provides: "1D composite-vs-single comparison output"
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-1d.json"
      provides: "Machine-readable 1D evidence inventory and metric extraction"
  key_links:
    - from: "artifacts/configs/composite-empirical-1d.yaml"
      to: "agent/backtest/composite_backtest_compare.py"
      via: "--config"
      pattern: "composite-empirical-1d.yaml"
    - from: "comparison run directories"
      to: "artifacts/evidence-1d.json"
      via: "run_card.json and artifacts inventory"
      pattern: "sharpe_ratio|total_return|win_rate|max_drawdown|trade_count"
---

<objective>
Execute the reproducible 1D empirical composite-vs-single comparison and inventory its evidence.

Purpose: Close the main empirical METR evidence for 2024-2026 daily timeframe without changing strategy behavior.
Output: 1D comparison report, per-variant run directories, and a machine-readable evidence inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-composite-strategy-backtest/09-SUMMARY.md
@.planning/phases/09-composite-strategy-backtest/09-SECURITY.md
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
@.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml
@agent/backtest/composite_backtest_compare.py
@agent/backtest/reporting/composite_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 1D composite-vs-single empirical comparison</name>
  <read_first>
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml
agent/backtest/composite_backtest_compare.py
.planning/phases/09-composite-strategy-backtest/09-SECURITY.md
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/comparison_report.md, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/mtes_supertrend/run_card.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/mtesv3_only/run_card.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/supertrend_only/run_card.json</files>
  <action>Run the exact 1D command recorded in `empirical-run-manifest.json`, using `.venv/bin/python` when available. The command must invoke `python -m backtest.composite_backtest_compare --config .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml --run-root .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d --timeout-seconds 300`. Preserve Phase 09 mitigations: list-form subprocess invocation, fixed strategy variant allowlist, safe run-root validation, timeout handling, output redaction/truncation, row/artifact caps, and env-only credentials. Do not change strategy parameters to improve results. Capture the process exit code and bounded stdout/stderr diagnostics into `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/run-status.json` whether the command succeeds or fails. A non-zero command exit must not erase diagnostics; it must be represented for Task 2 as `partial` or `blocked` evidence.</action>
  <verify>
    <automated>PY=.venv/bin/python; [ -x "$PY" ] || PY=python; "$PY" - <<'PY'
import json, os, subprocess, sys
from pathlib import Path
root = Path('.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts')
run_root = root / 'runs' / '1d'
run_root.mkdir(parents=True, exist_ok=True)
cmd = [
    sys.executable, '-m', 'backtest.composite_backtest_compare',
    '--config', str(root / 'configs' / 'composite-empirical-1d.yaml'),
    '--run-root', str(run_root),
    '--timeout-seconds', '300',
]
env = dict(os.environ)
env['PYTHONPATH'] = 'agent'
proc = subprocess.run(cmd, env=env, text=True, capture_output=True, timeout=360)
status = {
    'timeframe': '1D',
    'command': cmd,
    'exit_code': proc.returncode,
    'status': 'succeeded' if proc.returncode == 0 else 'partial_or_blocked',
    'stdout_tail': proc.stdout[-4000:],
    'stderr_tail': proc.stderr[-4000:],
}
(run_root / 'run-status.json').write_text(json.dumps(status, indent=2), encoding='utf-8')
if proc.returncode == 0:
    assert (run_root / 'comparison_report.md').exists(), 'missing comparison_report.md after successful run'
else:
    assert (run_root / 'run-status.json').exists(), 'missing run-status.json for failed run'
print('1d run status captured:', status['status'])
PY</automated>
  </verify>
  <acceptance_criteria>The 1D run either completes with `comparison_report.md` plus three variant `run_card.json` files, or produces a bounded failure artifact captured by Task 2; no security controls from Phase 09 are disabled or bypassed.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory 1D metrics and artifacts</name>
  <read_first>
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d/comparison_report.md
agent/backtest/reporting/composite_report.py
agent/backtest/metrics.py
.planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-1d.json</files>
  <action>Create `evidence-1d.json` by reading the 1D run root and recording: status (`verified`, `partial`, or `blocked`), config path, run root, strategy variant labels exactly `MTES+SuperTrend`, `MTESv3-only`, `SuperTrend-only`, per-variant run card paths, metrics for `total_return`, `win_rate`, `sharpe_ratio`, `max_drawdown`, and `trade_count`, existence of `artifacts/signals_key_nodes.csv` and `artifacts/signals_per_source.json`, data sources, date range, symbols attempted, and any limitations. If metric keys use aliases in run cards, normalize them in the evidence JSON while preserving original keys under `raw_metrics_keys`. Do not calculate new metrics from raw trades unless the run card is missing a required metric; if missing, record the metric as missing rather than inventing it.</action>
  <verify>
    <automated>.venv/bin/python - <<'PY'
import json
from pathlib import Path
root = Path('.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts')
evidence = json.loads((root / 'evidence-1d.json').read_text())
assert evidence['timeframe'] == '1D'
assert evidence['status'] in {'verified', 'partial', 'blocked'}
assert set(evidence['variants']) == {'MTES+SuperTrend', 'MTESv3-only', 'SuperTrend-only'}
required = {'total_return', 'win_rate', 'sharpe_ratio', 'max_drawdown', 'trade_count'}
if evidence['status'] == 'verified':
    for variant, payload in evidence['variants'].items():
        assert required <= set(payload['metrics']), (variant, payload.get('metrics'))
print('evidence-1d.json: PASS')
PY</automated>
  </verify>
  <acceptance_criteria>`evidence-1d.json` is valid JSON and gives the final 1D empirical status. When verified, every variant has the five required comparable metrics. When partial/blocked, the JSON states the exact blocker and points to bounded run diagnostics.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backtest config to subprocess | YAML config drives `backtest.composite_backtest_compare`, which launches runner subprocesses. |
| Run-root artifact writes | Empirical run artifacts are written under the Phase 10 artifact directory. |
| Market data into metrics | External/local OHLCV data become metrics, run cards, and CSV/JSON artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-S1 | Spoofing/Elevation | `run_dir/code/signal_engine.py` | mitigate | Preserve Phase 09 trusted signal-engine verification; do not edit runner trust controls. |
| T-10-02-T1 | Tampering | `--run-root` and config | mitigate | Use Phase 09 `safe_run_dir` validation and fixed `_VARIANTS` allowlist. |
| T-10-02-I1 | Information Disclosure | Subprocess output and evidence JSON | mitigate | Preserve redaction/truncation; evidence JSON must not include env secrets or raw credentials. |
| T-10-02-D1 | Denial of Service | Empirical comparison subprocesses | mitigate | Use timeout and row/artifact limit env controls; do not raise limits without recording why. |
| T-10-02-T2 | Tampering | CSV artifacts | mitigate | Preserve Phase 09 CSV safety controls for formula-trigger values. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
- `cd agent && ../.venv/bin/python -m backtest.composite_backtest_compare --config ../.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml --run-root ../.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/runs/1d --timeout-seconds 300`
- Parse `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/evidence-1d.json` and confirm required metrics or explicit blocker.
</verification>

<success_criteria>
- 1D empirical run evidence exists for composite, MTES-only, and SuperTrend-only variants.
- METR-01 and METR-02 are represented by comparable metrics in evidence JSON.
- METR-03 input artifacts are inventoried for report generation.
- RPT-01 has at least a generated comparison report or a documented blocked status.
</success_criteria>

<output>
Create `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/10-02-SUMMARY.md` when done.
</output>
