---
phase: 10-close-gap-data-rpt-empirical-composite-backtest-validation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml
  - .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - RPT-03
must_haves:
  truths:
    - "Executor can prove which watchlist instruments are eligible for empirical backtest evidence"
    - "Executor can prove 2024-2026 and 1D/4H coverage were attempted before running comparisons"
    - "Executor can run Phase 10 from fixed manifests/configs without inventing symbols or parameters"
  artifacts:
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json"
      provides: "Canonical Phase 10 run manifest with symbols, date range, intervals, commands, and limitations"
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml"
      provides: "Fixed 1D empirical comparison config"
    - path: ".planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml"
      provides: "Fixed 4H empirical comparison config or documented attempted config"
  key_links:
    - from: "watchlist/us_futures_watchlist.csv"
      to: "artifacts/empirical-run-manifest.json"
      via: "eligible symbol extraction and limitation notes"
      pattern: "GC=F|SI=F|CL=F"
    - from: "scripts/check_watchlist_data.py"
      to: "artifacts/data-readiness-*.json"
      via: "JSON data health evidence"
      pattern: "can_backtest"
---

<objective>
Establish Phase 10 empirical evidence inputs: data readiness evidence, fixed run manifests, and 1D/4H comparison configs for reproducible 2024-2026 composite-vs-single runs.

Purpose: Phase 10 closes DATA/RPT empirical evidence gaps without adding strategy logic. Executors must first prove what data is available and exactly what will be run.
Output: Data readiness JSON artifacts plus canonical 1D and 4H empirical configs under the Phase 10 artifact directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-composite-strategy-backtest/09-SUMMARY.md
@.planning/phases/09-composite-strategy-backtest/09-UAT.md
@.planning/phases/09-composite-strategy-backtest/09-SECURITY.md
@agent/backtest/composite_backtest_compare.py
@agent/backtest/configs/composite_backtest.yaml
@scripts/check_watchlist_data.py
@agent/src/data/watchlist.py
@watchlist/us_futures_watchlist.csv
@watchlist/etf_watchlist.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture data readiness for empirical watchlist coverage</name>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-composite-strategy-backtest/09-SECURITY.md
scripts/check_watchlist_data.py
agent/src/data/watchlist.py
watchlist/us_futures_watchlist.csv
watchlist/etf_watchlist.csv
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json</files>
  <action>Create the Phase 10 artifact directory and run the existing data-health CLI for `watchlist/us_futures_watchlist.csv` and `watchlist/etf_watchlist.csv` with `--format json` and `--json-output` into the two listed JSON files. Use `.venv/bin/python` if it exists, otherwise `python`. Preserve env-only credential behavior: do not write API keys, tokens, or credentials into artifacts. If either watchlist cannot fully backtest, keep the JSON output and let downstream plans treat it as partial/blocked evidence rather than silently replacing the source or adding a new data integration.</action>
  <verify>
    <automated>.venv/bin/python scripts/check_watchlist_data.py --watchlist watchlist/us_futures_watchlist.csv --data-dir data --format json --json-output .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json || test -s .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json</automated>
    <automated>.venv/bin/python scripts/check_watchlist_data.py --watchlist watchlist/etf_watchlist.csv --data-dir data --format json --json-output .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json || test -s .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json</automated>
  </verify>
  <acceptance_criteria>Both readiness JSON files exist, are non-empty valid JSON, preserve the CLI's actual `can_backtest` or blocking status, and contain no credential-like strings such as `api_key`, `token=`, `secret=`, or `Bearer `.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

<task type="auto">
  <name>Task 2: Create fixed empirical run manifest and 1D/4H configs</name>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-composite-strategy-backtest/09-SUMMARY.md
agent/backtest/composite_backtest_compare.py
agent/backtest/configs/composite_backtest.yaml
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-us-futures.json
.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/data-readiness-etf.json
  </read_first>
  <files>.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/empirical-run-manifest.json, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-1d.yaml, .planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts/configs/composite-empirical-4h.yaml</files>
  <action>Create a deterministic `empirical-run-manifest.json` that records `phase`, `date_range` exactly `2024-01-01` to `2026-01-01`, `source` as the existing Phase 09 source unless data readiness requires a documented partial status, `intervals_attempted` as `1D` and `4H`, eligible symbols selected from the existing watchlists, excluded symbols with explicit data-readiness reasons, and the exact `python -m backtest.composite_backtest_compare` commands to run in Plans 10-02 and 10-03. Create `composite-empirical-1d.yaml` and `composite-empirical-4h.yaml` by copying the Phase 09 config shape: same MTES v3 + SuperTrend parameters, same fixed strategy variants, same 2xATR settings, date range 2024-2026, and `interval` set to `1D` or `4H`. Do not tune parameters or introduce new strategy logic. If 4H data availability is blocked, still create the 4H config as an attempted config and record the block in manifest limitations.</action>
  <verify>
    <automated>.venv/bin/python -m py_compile agent/backtest/composite_backtest_compare.py agent/backtest/configs/signal_engine.py</automated>
    <automated>.venv/bin/python - <<'PY'
import json, yaml
from pathlib import Path
base = Path('.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/artifacts')
manifest = json.loads((base / 'empirical-run-manifest.json').read_text())
assert manifest['date_range']['start'] == '2024-01-01'
assert manifest['date_range']['end'] == '2026-01-01'
assert {'1D', '4H'} <= set(manifest['intervals_attempted'])
assert manifest.get('commands'), 'manifest must list execution commands'
for name, interval in [('composite-empirical-1d.yaml', '1D'), ('composite-empirical-4h.yaml', '4H')]:
    cfg = yaml.safe_load((base / 'configs' / name).read_text())
    assert cfg['start_date'] == '2024-01-01'
    assert cfg['end_date'] == '2026-01-01'
    assert cfg['interval'] == interval
    assert cfg.get('compare_single') is True
    assert cfg.get('atr_multiplier') == 2.0
    assert cfg.get('codes'), f'{name} must contain at least one attempted code or manifest must explain block'
print('phase10 manifest/configs: PASS')
PY</automated>
  </verify>
  <acceptance_criteria>The manifest and both configs parse successfully; they use the fixed Phase 09 strategy variants and 2xATR controls; they explicitly distinguish eligible, excluded, partial, and blocked symbols/timeframes for DATA-01, DATA-02, DATA-03, and RPT-03.</acceptance_criteria>
  <done>Acceptance criteria met and automated verification passes or records an explicit partial/blocked status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watchlist/data-health input | Existing CSV watchlists and local data-health JSON cross into Phase 10 empirical evidence artifacts. |
| Config boundary | YAML configs created here will drive backtest subprocesses in later plans. |
| Artifact boundary | Data-readiness and manifest artifacts are written under `.planning/phases/10.../artifacts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-T1 | Tampering | Empirical configs | mitigate | Use existing `yaml.safe_load` path in `composite_backtest_compare.py`; do not introduce eval/pickle; keep fixed variant allowlist from Phase 09. |
| T-10-01-I1 | Information Disclosure | Readiness artifacts | mitigate | Preserve env-only credentials; verify artifacts contain no common secret tokens before acceptance. |
| T-10-01-R1 | Repudiation | Manifest provenance | mitigate | Manifest records watchlist source files, data-health artifact paths, exact commands, and limitations. |
| T-10-01-D1 | Denial of Service | Later empirical runs | mitigate | Manifest commands preserve Phase 09 subprocess timeout and row-limit env controls; no row/artifact caps disabled. |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned in this closure phase. |
</threat_model>

<verification>
Run these after tasks complete:
- `.venv/bin/python -m py_compile agent/backtest/composite_backtest_compare.py agent/backtest/configs/signal_engine.py`
- Parse `artifacts/empirical-run-manifest.json` and both YAML configs with Python JSON/YAML loaders.
- Confirm both intervals `1D` and `4H` are represented as attempted evidence.
</verification>

<success_criteria>
- DATA-01 is planned with fixed 2024-01-01 to 2026-01-01 date range.
- DATA-02 is planned from existing watchlists with explicit included/excluded/blocked status.
- DATA-03 is planned with both 1D and 4H configs; blocked 4H is documented rather than omitted.
- RPT-03 has input data-quality evidence ready for downstream report generation.
</success_criteria>

<output>
Create `.planning/phases/10-close-gap-data-rpt-empirical-composite-backtest-validation/10-01-SUMMARY.md` when done.
</output>
